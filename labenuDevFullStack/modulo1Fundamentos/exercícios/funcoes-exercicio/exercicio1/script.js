//a)Declare uma função que imprima `Olá, [SEU NOME]!`. Chame esta função.

/* let nomeUsuario = prompt('Digite seu nome');

function chamaNome(Nome) {
	return `Olá, ${nomeUsuario}`;
} */

/* Ver se esta certo

let retornoFuncao = chamaNome(nomeUsuario);
console.log(retornoFuncao);

*/

//b)Declare uma função que receba um número como parâmetro imprima no console a tabuada do dele. Chame esta função várias vezes com números diferentes.

/* const numero = Number(prompt('Digite um Número'));

function recebeNumero(numero) {
	const tabuada = [
		numero * 1,
		numero * 2,
		numero * 3,
		numero * 4,
		numero * 5,
		numero * 6,
		numero * 7,
		numero * 8,
		numero * 9,
		numero * 10,
	];
	return `Essa é a tabuada do número ${numero}:\n${tabuada}`;
}
 */

/* verificar se esta certo
const verTab = recebeNumero(numero);

console.log(verTab); */

/* c) Comente as funções criadas acima, e reescreva-as utilizando expressões de função, ou __arrow functions__  */

/* let nomeUser2 = prompt('Digite seu nome');

const nome = (nomeUser2) => {
	return `Olá, ${nomeUser2}`;
}; */

/* const verifCert = nome(nomeUser2);

console.log(verifCert); */

/* const numero = Number(prompt('Digite um Número'));

const recebeNumero = (numero) => {
	const tabuada = [
		numero * 1,
		numero * 2,
		numero * 3,
		numero * 4,
		numero * 5,
		numero * 6,
		numero * 7,
		numero * 8,
		numero * 9,
		numero * 10,
	];
	return `Essa é a tabuada do número ${numero}:\n${tabuada}`;
}; */

/* const verTab = recebeNumero(numero);

console.log(verTab); */
